{
  "name": "whatsapp-bot-advanced",
  "version": "2.0.0",
  "description": "Bot WhatsApp haute performance avec code de connexion et système d'auth",
  "main": "bot.js",
  "scripts": {
    "start": "node --max-old-space-size=512 bot.js",
    "dev": "nodemon bot.js",
    "cleanup": "node -e \"require('fs').writeFileSync('users_data.json', '{}')\"",
    "backup": "cp users_data.json backup_$(date +%s).json"
  },
  "dependencies": {
    "whatsapp-web.js": "^1.21.0",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": [
    "whatsapp",
    "bot",
    "performance",
    "render",
    "connection-code"
  ],
  "author": "WhatsApp Bot Pro",
  "license": "MIT"
}
